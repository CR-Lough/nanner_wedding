<!DOCTYPE html>
<html>
<head>
  <title>Our Wedding Photo Gallery</title>
  <style>
    /* Your existing styles */

    /* Add new or modified styles here */
    header {
      background-color: #333;
      color: #fff;
      padding: 10px 0;
      text-align: center;
    }

    footer {
      background-color: #333;
      color: #fff;
      padding: 10px 0;
      text-align: center;
    }
  </style>
</head>
<body>

<header>
  <h1>Our Wedding Photo Gallery</h1>
</header>

<div class="container">
  <%= form_for(@post, html: { multipart: true }) do |form| %>
    <label for="post_title">Title:</label>
    <%= form.text_field :title %>

    <label for="post_images">Select photos to upload:</label>
    <%= form.file_field :images, multiple: true %>
  
    <%= form.submit "Create Post" %>
  <% end %>

  <% @posts.each do |post| %>
    <h2><%= post.title %></h2>
    <ul>
      <% post.images.each do |image| %>
        <li>
          <% if image.representable? %>
            <%= image_tag image.representation(resize_to_limit: [300, 300]) %>
          <% else %>
            <div class="placeholder-image">
              <%= image_tag "placeholder.png", alt: "Download image" %>
            </div>
          <% end %>
        </li>
      <% end %>
    </ul>
  <% end %>
</div>

<footer>
  <p>&copy; <%= Time.now.year %> Our Wedding | All rights reserved</p>
</footer>

</body>
</html>
